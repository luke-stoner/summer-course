import math

def rectangle_area(l: float, w: float) -> float:
    return l*w
def circle_area(r: float) -> float:
    return math.pi * r**2
def tri_area(b: float, h: float) -> float:
    return 0.5*b*h
